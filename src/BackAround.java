public class BackAround {
    public static void backAroundResolution() {
        System.out.println("BackAround " + DisplayFunc.word.substring(DisplayFunc.word.length()-1) + DisplayFunc.word + DisplayFunc.word.substring(DisplayFunc.word.length()-1));
    }
}
